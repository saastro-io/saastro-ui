node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example
.astro/
apps/ui-docs/.wrangler/
.playwright-mcp/

# Salida generada del registry (shadcn build) — la única copia viva va a dist/
apps/ui-docs/public/r/
packages/ui-registry/public/r/

# Artefactos locales de saastro-relay: `knowledge/` se REGENERA en cada sync
# (INDEX.md lleva un `last_sync` que cambia solo) y ensuciaba todos los commits.
# Mismo criterio que hub, platform y forms.
.saastro-relay
knowledge/
